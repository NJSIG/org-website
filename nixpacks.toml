[phases.build]
cmds = ["next build --experimental-build-mode compile"]

[start]
cmds = "export NEXT_PUBLIC_IMAGE_OPTIMIZATION_API=$NEXT_PUBLIC_IMAGE_OPTIMIZATION_API && next build --experimental-build-mode generate && next start"
